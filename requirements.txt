# MH-FLOCKE — Python Dependencies
# =================================
# Install: pip install -r requirements.txt
# For GPU (RunPod): bash scripts/runpod_setup.sh

# ── Core Physics ──
mujoco>=3.5.0
numpy
scipy

# ── Deep Learning ──
# CPU-only install (avoids the ~2 GB CUDA build):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ── Visualization + Video ──
Pillow
matplotlib
networkx

# ── Mesh Generation ──
numpy-stl

# ── Config / Serialization ──
pyyaml
orjson
msgpack

# ── SNN Performance ──
numba

# ── NLP / Brain ──
sentence-transformers
spacy

# ── LLM Adapters ──
litellm

# ── Web Search ──
ddgs

# ── Vector Database ──
lancedb

# ── System Monitoring ──
psutil

# ── Web Dashboard (FLOG analysis server) ──
flask             # HTTP server for the FLOG analysis dashboard (flog_server.py)

# ── WebSocket (live dashboard + Bittle bridge) ──
websockets        # async server for live dashboard streaming
websocket-client  # sync client for the Bittle WiFi bridge (bridge_bittle_wifi)

# ── Optional: GPU Physics Batch ──
# jax[cuda12]
# mujoco-mjx
